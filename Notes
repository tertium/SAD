- устроить нормальные имена переменных в markedF

- генерический specDef

- парсеры в Import/ допускают произвольные имена для символов!
  Возможны коллизии имен связанных переменных и символов в Export/
  Кроме того, убедиться, что каждый вложенный roundF (пока их нет)
  перехватывает индекс у внешнего, иначе коллизии неизбежны.

- добавить выключатель для поиска сведений (как?! чистая функция)

- перейти на Date.Time

- функции get_name в Pattern.hs не учитывают положение аргументных
  мест, возможны коллизии. Например (!x) и (x!).

######### учтенные заметки

- реализовать разбор случаев редукцией формульного образа случая при
  добавлении в контекст: (DCH:A -> B) => (!A) -- исключающий разбор.

  Делать это там же, где будет удаляться гипотеза индукции (DIH:).

  Альтернативы: дополнять мотиватор (неясно как), чистить контекст
  перед завершением разбора (неуклюже), заменять вхождения thesis 
  на ложь при добавлении в контекст (пачкает семантику текста, 
  даже если безопасно, к тому же мешает редукции тезиса).

- аннотации к множествам содержат вложенные кванторы по одной переменной
  (ничего страшного, это скрытые переменные, так что мотиватору плевать)

- убедиться, что при замене zThesis на текущий тезис не возникает
  коллизий переменных, которые помешают мотиватору
  (порядок, мы снимаем кванторы только в тезисе, а в нем коллизий нет)


######### старые заметки:

- понимать "set X of natural numbers" как 
    "X is a set and every element of X is a natural number"
  отличать ли "the set of natural numbers"? "set of all natural numbers"?

- выносить наружу контекст из подпонятия в "set of"

- собирать сведения под кванторами существования, при условии,
  что эти сведения не будут содержать сколемовских символов

- сколемизировать посылки (???)

- научиться мотивировать предположения вида "Let x = t" при текущем тезисе
  вида Th[t], редуцируя тезис к Th[x]. (Необходимо предварительно вытащить
  из предположения все декларирующие равенства вместе с переменными, а затем
  уточнить tmPass : пусть ищет совпадения внутри термов. Вопрос: а если у нас
  тезис как раз имеет вид "forall x' = t ..."?

- дописать деструктивное раскрытие

